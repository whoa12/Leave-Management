public class StringMethods {
    public static void main(String[] args) {


        String name = "Laptop";
      //  int value = name.length(); gives the number of letter used in string
     //   String lstring = name.toLowerCase(); converts all the letters to its smaller form.
      // String ustring = name.toUpperCase(); converts strings to their upper form(capital)
         //System.out.println(ustring);




    }
}